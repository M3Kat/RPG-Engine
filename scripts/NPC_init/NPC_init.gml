/// @description Set default values for NPC

enum _npcAnimState
{
	idle,
	walk,
	enum_end
}

enum _npcAnimData
{
	sprite,
	animSpeed,
	enum_end
}

enum _npcOrientation
{
	up,
	right,
	down,
	left,
	enum_end
}